# ══════════════════════════════════════════════════════════════════════════════
#  Gestor JAL — Variables de entorno del backend en Render (modo híbrido)
#  Referencia para llenar el dashboard de Render (o render.yaml) — este archivo NO
#  se sube como .env real, es solo documentación de qué poner y por qué.
# ══════════════════════════════════════════════════════════════════════════════

NODE_ENV=production
PORT=3001

# ── Base de datos — Supabase (Postgres administrado) ────────────────────────────
# Usa el connection string del "Session pooler" de Supabase (puerto 5432, compatible
# con IPv4) — no el host directo db.<ref>.supabase.co, que en varias regiones solo
# resuelve por IPv6 y Render no lo alcanza. Dashboard del proyecto → Connect →
# "Session pooler". DB_SSL=true porque Supabase lo exige.
DATABASE_URL=postgresql://postgres.TU_REF:CAMBIAR_CONTRASEÑA@aws-0-TU_REGION.pooler.supabase.com:5432/postgres
DB_SSL=true

# ── Seguridad ────────────────────────────────────────────────────────────────
# Genera con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIAR_clave_aleatoria_minimo_64_caracteres_hex
ENCRYPTION_KEY=CAMBIAR_clave_aleatoria_minimo_32_caracteres_hex

# ── CORS ─────────────────────────────────────────────────────────────────────
# Dominio(s) del frontend en Vercel — sin barra final, separados por coma si hay
# varios (ej. producción + preview deployments).
ALLOWED_ORIGINS=https://TU_FRONTEND.vercel.app

# ── Tailscale — solo necesario para el chat IA (Ollama) ─────────────────────────
# La base de datos ya NO pasa por Tailscale (vive en Supabase). Deja TS_AUTHKEY
# vacío si no usas el chat. Auth key reusable, generar en:
# https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=
TS_HOSTNAME=gestor-jal-backend
# IP/MagicDNS de Tailscale del PC (solo si usas el chat) — destino del forwarder de Ollama.
PROXY_TARGET_HOST=

# ── Almacenamiento ───────────────────────────────────────────────────────────
# El disco del backend en Render (free tier) es efímero — estas rutas son solo
# caché local mientras el contenedor vive; la copia durable vive en Supabase
# (columnas file_docx/file_pdf, ver backend/migrations/20260101000025-*).
STORAGE_PATH=/app/data/output
TEMPLATES_PATH=/app/plantillas

# ── Identidad de la JAL ───────────────────────────────────────────────────────
JAL_NOMBRE=JAL Comuna 12 - La América - Medellín
JAL_NOMBRE_CORTO=JAL C12

# ── Chat IA — Ollama corriendo en el PC local (opcional) ───────────────────────
# Igual que DATABASE_URL: apunta a 127.0.0.1, NO a la IP de Tailscale directamente.
# entrypoint.sh levanta un segundo forwarder (mismo mecanismo que
# tailscale-db-proxy.js usa para Postgres) que reenvía 127.0.0.1:11434 por el
# SOCKS5 hacia PROXY_TARGET_HOST:11434. Requiere que Ollama en el PC escuche en
# la red (OLLAMA_HOST=0.0.0.0:11434), no solo en 127.0.0.1 (que es el default).
# Deja ambas vacías si no usas el chat.
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2:1b

# ── Backup Google Drive (opcional) ──────────────────────────────────────────
# El redirect URI debe actualizarse en Google Cloud Console con la URL pública
# de Render (no localhost).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://TU_BACKEND.onrender.com/v1/backup/drive/callback
